(* My first TeaC program.
File: myprog.tc *)

-- Constant
const message[14] <- "Hello world!\n": string;

const start <- (): int => {
	i <- k ^ 2;
	writeString(message);
	let i <- 5 : int; -- Variable
	writeInt(i);
}
